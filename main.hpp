#include <cmath>

using namespace std;

class Person {
    public:
    string first_name = "Artem";
    string second_name = "Danilov";
    int age = 23;
    Person(string first_name, string second_name, int age){
        this->first_name = first_name;
        this->second_name = second_name;
        this->age = age;
     }
     ~Person();
    private:
    const void(int age){Person.age = age}
}