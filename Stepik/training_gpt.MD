1.	Функция суммирования элементов массива
Напиши функцию, которая принимает массив целых чисел и его размер, а затем возвращает сумму всех элементов массива.
int sum(int *array, size_t size);

2. Поиск минимального элемента в массиве
Напиши функцию, которая принимает массив целых чисел и его размер, а затем возвращает минимальный элемент массива.
int array_min(int *array, size_t size);

3. Перевод строки в верхний регистр
Напиши функцию, которая принимает строку и изменяет все символы на заглавные (используй стандартные функции работы со строками и символами, такие как toupper).
void to_upper_case(char *str);

4. Напиши функцию, которая принимает строку и изменяет ее, инвертировав порядок символов.
void reverse_string(char *str);

5. Проверка, является ли число простым
Напиши функцию, которая принимает целое число и возвращает 1, если оно простое, и 0, если нет.
int is_prime(int n);

6. Напиши функцию, которая умножает две матрицы размером n x n и возвращает результат. Учитывай, что для матриц размером n x n умножение возможно только в случае, если количество столбцов первой матрицы равно количеству строк второй матрицы.
void multiply_matrices(int *A, int *B, int *C, size_t n);

7. Поиск элемента в отсортированном массиве (бинарный поиск)
Напиши функцию, которая использует бинарный поиск для нахождения элемента в отсортированном массиве. Если элемент найден, возвращай его индекс, если нет — возвращай -1.
int binary_search(int *array, size_t size, int target);

8. Алгоритм слияния двух отсортированных массивов
Напиши функцию, которая сливает два отсортированных массива в один отсортированный массив.
void merge_sorted_arrays(int *array1, size_t size1, int *array2, size_t size2, int *result);

9. Удаление всех пробелов из строки
Напиши функцию, которая удаляет все пробелы из строки.
void remove_spaces(char *str);

10. Функция вычисления факториала (рекурсивно)
Напиши рекурсивную функцию для вычисления факториала числа.
int factorial(int n);