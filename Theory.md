Statements, expressions
    Программы на C пишутся в виде текста, который называется исходный код. Он может быть разбит на множество файлов, обычно у них расширение .c . 
        Когда эти файлы пропускаются через специальную программу -- компилятор -- они становятся набором машинных инструкций, которые компьютер может выполнять напрямую.

Statement — это команда, инструкция, действие
Исходный код на C описывает последовательность выполнения действий. 
    Поэтому в каждой программе вам придётся привести шаги, которые приведут её к цели. 
        Каждый шаг это команда: "Сделай то! Сделай это!" Отсюда и название такого стиля программирования: императивный. 

        ... statement;
        one more statement;
        ...


Expression — это выражение, подсчёт чисел или других данных
    Второй важный тип кусочков программ называется expression (выражение). Они описывают вычисления, в которых есть конечный результат, то есть некоторые данные. 

Вот примеры выражений:

42   посчитается как 42
4 + 9 посчитается как 13
2 * (3 + 1) посчитается как 8
2 * 3 + 1 посчитается как 7
11 / 5 посчитается как 2, потому что деление двух целых чисел считается целочисленным.
11 % 5 посчитается как 1,  это остаток от целочисленного деления 11 на 5
    Арифметические действия имеют привычный нам приоритет: сначала умножение и деление, потом сложение и вычитание.

Выражения могут быть частью statement'ов, что мы сейчас увидим.

Функции

Функция это часть исходного кода программы, описывающая действия, направленные на достижение единой цели. У функции есть имя, отражающее эту цель, и тело, состоящее из самих вычислений. 
Ключевое слово void означает, что функция служит всего лишь контейнером для нескольких statement'ов. Функцию пишут чтобы дать последовательности действий имя -- в данном случае, это имя proc. Тело всегда пишется { в фигурных скобках }.

void <имя функции>() {
   <тело функции>
}

Нельзя писать statement'ы вне функций!
Так нельзя:

printf("Something");


void proc() {
   ...
}

Вызов функций
Дав имя кусочку кода мы можем обратиться к нему по имени, написав её имя и скобки.

Это называется вызвать функцию.

Один из предыдущих примеров выглядел так:
void proc() {
   printf("Hello!");                // Тело функции состоит
   printf("\n");                    // из этих трёх statement'ов
   printf("From ITMO university");  // Каждый завершается точкой с запятой
}
Это было описание функции, оно показывает, что нужно будет сделать когда функцию вызовут.

Вызов этой функции выглядит так:

proc();

Обратите внимание: при вызове функции мы не писали слова void! Оно является частью описания функции, но не нужно для ее вызова.

Функции с одним аргументом:

void print_int( int x ) {
//              ^^^^^ добавили в скобки тип аргумента и его имя

   printf("%d", x );
}  //          ^^^  заменили 40 + 2 на x

void main() {

   print_int( 40 + 2 );
//           ^^^ указали значение для аргумента `x` в месте вызова 
}


Функции с несколькими аргументами
Функции с несколькими аргументами выглядят похоже: вместо одного аргумента в скобках мы указываем несколько аргументов через запятую. 

Вот функция с одним аргументом arg; она принимает число и печатает его на экран.

void print_int(int arg) {
   printf("%d", arg);
}
А вот функция с двумя аргументами arg1 и arg2. Она принимает два числа и печатает первое число, а затем второе число на новой строке.

void print_int2(int arg1, int arg2) {
   printf("%d", arg1);
   printf("\n");
   printf("%d", arg2);
}
Например, если вызвать эту функцию с аргументами 10 и 33:

print_int2( 10, 33 );
... то на экран выведется текст:

10
33

Возвращаемое значение функции
Код внутри функции служит единой цели, а имя функции мы выбираем так, чтобы оно описывало эту цель. Зачастую функции пишут чтобы вычислять какие-то значения. Это подсчитанное значение мы можем вернуть из функции в другую функцию, которая её вызвала. Это называется вернуть значение из функции, а само значение называется возвращаемым значением.

Посмотрим на примере, как возвращать значения из функций. Функция sum вычисляет сумму двух чисел, а вся программа считает сумму чисел 4 и 3 и выводит сумму на экран.

int sum(int x, int y) {
   return x + y;
}

void main() {
   printf("%d", sum(4, 3) );
}
Новая функция, которую мы назвали sum, отличается от функций, которые мы рассматривали ранее:

вместо ключевого слова void указан тип возвращаемого значения: int ;
в функции присутствует специальное предложение return <expr>, где <expr> -- произвольное выражение. При его выполнении функция сразу завершает свою работу и возвращает результат вычисления <expr>. 
вызов функции теперь можно использовать в качестве выражения: вместо вызова функции подставляются данные, которые она вычислила. Разумеется, так как эта функция считает число, то вызывать ее можно только там, где мы бы могли вписать вместо вызова конкретное число.

А что после return?
Теперь добавим в функцию sum вывод строки после return.

int sum(int x, int y) {
   return x + y;
   printf("computed");
}

void main() {
   printf("%d", sum(4, 3) );
}
Однако надпись computed мы никогда не увидим. Как только встречается конструкция return, функция sum сразу возвращает значение, и все последующие строки в теле функции sum игнорируются. Dead code! ☠️

Подведём итог. В общем случае мы объявляем функцию по следующему шаблону:

тип_возвращаемого_значения имя_функции (тип1 имя_параметра1, тип2 имя_параметра2...) {
   тело функции 
}

if-statement
Зачастую нам необходимо выполнить не все инструкции в программе. Если некоторое условие истинно, то мы выполняем одни инструкции, иначе -- другие. Для этого предназначен специальный statement if. Вот два примера его использования:

if (42)    
//  ^^ условие
{                     // ветка then
   printf("Hello!");  // ветка then
}                     // ветка then
else 
{                     // ветка else
   printf("Not good");// ветка else 
}

Для кодирования составных логических условий нам потребуются логические связки между ними, так же, как в математической логике.  Связки пишутся между двумя логическими значениями и возвращают 0 или 1. 

Логическое И &&. Вернет 1 если оба числа ненулевые.
x && y равно 1 если x != 0 и y != 0, иначе 0.
Логическое ИЛИ ||. Вернет 1 если хотя бы одно число ненулевое.
x || y равно 0 если x == 0 и y == 0, иначе 1.
Также есть одна логическая операция, которая применяется к одному выражению, а не к двум:

Логическое НЕ !
!x равно 1 если  x == 0, иначе 0

Помните, что для C истина и ложь -- это всего лишь проверка на равенство нулю. Поэтому логические связки можно писать и между числами, просто нули будут считаться ложью, а ненулевые числа -- истиной. Например, 8 && 2 == 1 т.к. и 8 и 2 являются истинными значениями.  В языке Java, например, нельзя написать так:

if (3 && 9) {
  //какой-то код
}
ПРИОРИТЕТЫ

Умножение и деление перед сложением и вычитанием.
&& перед ||
Сравнения (<, >, >= и т.д.) перед && и ||


x	y	x && y	x || y
0	0	   0	   0
0	1	   0	   1
1	0	   0	   1
1	1	   1	   1

Мы можем описать смысл этих операций так:

логическое И ложно если хотя бы один из операндов ложный; 
логическое ИЛИ истинно если хотя бы один из операндов истинный.

int print1() { printf("Hello!"); return 1; }


...
0 || print1() ; // напечатает Hello!
1 || print1() ; // ничего не напечатает: print1 не был запущен


print1() || print1() ; // напечатает Hello! один раз; второй print1 не будет запущен. 


int print0() { printf("Hello!"); return 0; }

...
1 && print0() ; // напечатает Hello!
0 && print0() ; // ничего не напечатает: print0 не был запущен


print0() && print0() ; // напечатает Hello! один раз; второй print0 не будет запущен. 